@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container-fuild mb-2">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover Types</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" asp-controller="CoverType" class="btn btn-outline-primary">
                <i class="bi bi-plus-circle"></i>
                Create a new cover type
            </a>
        </div>

    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Cover Type Name</th>
            <th>Function</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var coverType in Model)
        {
            <tr>
                <td>@coverType.Name</td>
                <td>
                    <div class="btn-group d-flex justify-content-center" role="group">
                        <a class="btn btn-success mx-2" asp-action="Update" asp-controller="CoverType" asp-route-id="@coverType.Id">
                            <i class="bi bi-pencil-square"></i>
                            Update
                        </a>
                        <a class="btn btn-warning mx-2" asp-action="Delete" asp-controller="CoverType" asp-route-id="@coverType.Id">
                            <i class="bi bi-trash"></i>
                            Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
